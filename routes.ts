export const publicroutes: string[] = [];

export const privateroutes: string[] = [];

export const authroutes: string[] = ["/auth-sign-in"];

export const apiauthprefixes: string = "/api/auth";

export const DEFAULT_REDIRECT_URL = "/";
